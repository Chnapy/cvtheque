<div class="fos_user_user_show">
  <ul>
    {% if user.cvFile is not null %}
      <li><a href="{{ user.cvFile.webPath }}" >Voir le cv (PDF)</a></li>
    {% endif %}
    {% if user.logBookFile is not null %}
      <li><a href="{{ user.logBookFile.webPath }}" >Voir le journal (PDF)</a></li>
    {% endif %}
  </ul>
  <div style="float: left; margin-right: 1em;">
    {% if user.image is not null %}
      <img src="{{ user.image.webPath }}" alt="{{ user.image.alt }}" height="60">
    {% endif %}
  </div>
  <h1>{{ 'profile.show.username'|trans({'%username%': user.username }, 'MGUserBundle') }}</h1>
  <p>{{ 'profile.show.country'|trans({'%country%': user.address.country|country }, 'MGUserBundle') }}</p>
  <p>{{ ('profile.show.birthday.'~user.gender)|trans({'%birthday%': user.birthday|date('d/m/Y'), '%age%': user.age}, 'MGUserBundle') }}</p>
  <h2>
    {{ user.titleProfile }}
  </h2>
  
  <article class="well" style="clear: both;">
    {{ user.description|markdown }}
  </article>
  <div class="well well-small">
    {% if user.hobbies.count > 0 %}
    <h3>Liste de passe-temps :</h3>
    <ul>
      {% for hobby in user.hobbies %}
        <li>{{ hobby.name }};</li>
      {% endfor %}
    </ul>
    {% endif %}
  </div>
  {% if user.id == app.user.id %}
  <ul>
  <li><a href="{{ path('fos_user_profile_edit') }}" class="btn">
    <i class="icon-edit"></i>
    Modifier le profil
  </a></li>
  <li><a href="{{ path('mg_applicant_profile_cv_upload') }}" class="btn">
    <i class="icon-edit"></i>
    Ajouter un CV (PDF)
  </a></li>
  <li><a href="{{ path('mg_applicant_profile_log_book_upload') }}" class="btn">
    <i class="icon-edit"></i>
    Ajouter un journal (PDF)
  </a></li>
  <li><a href="{{ path('mg_user_profile_picture_edit') }}" class="btn">
    <i class="icon-edit"></i>
    Modifier la photo
  </a></li>
  <li><a href="{{ path('mg_user_profile_delete') }}" class="btn">
    <i class="icon-trash"></i>
    Supprimer 
  </a></li>
  </ul>
  {% endif %}
  <p>{{ 'profile.show.created'|trans({'%date%': user.created|localizeddate('full', 'none')}, 'MGUserBundle') }}</p>
  <p>{{ 'profile.show.updated'|trans({'%date%': user.created|localizeddate('full', 'none')}, 'MGUserBundle') }}</p>    
</div>